<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.geonpil.mapper.CommentMapper">

    <resultMap id="CommentResultMap" type="com.geonpil.domain.Comment">
        <id property="commentId" column="comment_id"/>
        <result property="postId" column="post_id"/>
        <result property="userId" column="user_id"/>
        <result property="nickname" column="nickname"/>
        <result property="content" column="content"/>
        <result property="isDeleted" column="is_deleted"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="parentId" column="parent_id"/>
        <result property="replyCount" column="reply_count"/>
    </resultMap>


    <!-- 1. 댓글 등록 -->
    <insert id="insertComment"
            parameterType="com.geonpil.domain.Comment"
            useGeneratedKeys="true"
            keyProperty="commentId">
        INSERT INTO comments (
        post_id,
        user_id,
        parent_id,
        nickname,
        content,
        is_deleted,
        created_at,
        updated_at
        ) VALUES (
        #{postId},
        #{userId},
        #{parentId},
        #{nickname},
        #{content},
        false,
        NOW(),
        NOW()
        )
    </insert>



    <!-- 2. 댓글 목록 조회 -->
    <!-- 대댓글 기능 -->
    <select id="getCommentsByPostId" parameterType="long" resultMap="CommentResultMap">
        SELECT c.*
             ,  (SELECT COUNT(*) FROM comments r WHERE r.parent_id = c.comment_id AND r.is_deleted = false) AS reply_count
        FROM comments c
        WHERE post_id = #{postId}
        ORDER BY
        CASE WHEN parent_id IS NULL THEN comment_id ELSE parent_id END,
        created_at
    </select>

    <!-- 3. 댓글 soft delete -->
    <update id="softDeleteComment"
            parameterType="long">
        UPDATE comments
        SET is_deleted = true,
        updated_at = NOW()
        WHERE comment_id = #{commentId}
    </update>

    <!-- 댓글 아이디로 댓글 찾기 -->
    <select id="findById" parameterType="long" resultMap="CommentResultMap">
        SELECT * FROM comments
        WHERE comment_id = #{commentId}
    </select>

    <!-- 댓글수정 -->
    <update id="updateComment">
        UPDATE comments
        SET content = #{content}, updated_at = NOW()
        WHERE comment_id = #{id}
    </update>
</mapper>