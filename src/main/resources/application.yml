spring:
  datasource:
    url: jdbc:mysql://localhost:3306/geonpil?serverTimezone=Asia/Seoul
    username: root
    password: clzlsrnxk15
    driver-class-name: com.mysql.cj.jdbc.Driver

  devtools:
    restart:
      enabled: true  # 서버 코드 변경 시 자동 재시작
      trigger-file: .trigger
    livereload:
      enabled: true  # HTML/CSS/JS 실시간 반영

  web:
    resources:
      cache:
        period: 0  # 정적 리소스 캐시 무효화

  thymeleaf:
    cache: false  # 템플릿 캐시 무효화
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML

  mail:
    host: smtp.naver.com
    port: 465
    username: lwd33021@naver.com
    password: "CY2WGGLJ8LNH"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            enable: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.geonpil
  map-underscore-to-camel-case: true


logging:
  level:
    org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter: INFO