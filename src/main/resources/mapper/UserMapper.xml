<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.geonpil.mapper.UserMapper">

    <insert id="insertLocalUser" parameterType="com.geonpil.domain.User">
        INSERT INTO users (email, password, nickname, role)
        VALUES (#{email}, #{password}, #{nickname}, #{role})
    </insert>


    <insert id="insertSocialUser" parameterType="com.geonpil.domain.User">
        INSERT INTO users (email, nickname, provider, provider_id, role)
        VALUES (#{email}, #{nickname}, #{provider}, #{providerId}, #{role})
    </insert>


    <!-- 소셜 email 과 provider_id 로 확인-->
    <select id="findByProviderAndProviderId" resultType="User">
        SELECT * FROM users
        WHERE provider = #{provider} AND provider_id = #{providerId}
        LIMIT 1
    </select>


    <!-- 이메일 중복 확인 및 로그인 -->
    <select id="findByEmail" parameterType="string" resultType="com.geonpil.domain.User">
        SELECT * FROM users WHERE email = #{email} AND provider IS NULL
    </select>

    <!-- 닉네임 중복 확인 -->
    <select id="findByNickname" parameterType="string" resultType="com.geonpil.domain.User">
        SELECT * FROM users WHERE nickname = #{nickname}
    </select>


    <update id="updatePasswordByEmail">
        UPDATE users
        SET password = #{password}
        WHERE email = #{email}
    </update>

    <!-- 유저 닉네임 가지고 오기  -->
    <select id = "getUserNicknameById" parameterType="long" resultType="string">
        SELECT nickname
        FROM users
        WHERE id = #{id}
    </select>


</mapper>