<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.geonpil.mapper.book.BookMapper">

    <resultMap id="BookResultMap" type="com.geonpil.domain.entity.BookEntity">
        <id property="bookId" column="book_id"/>
        <result property="isbn" column="isbn"/>
        <result property="title" column="title"/>
        <result property="authors" column="authors"/>
        <result property="publisher" column="publisher"/>
        <result property="thumbnail" column="thumbnail"/>
        <result property="contents" column="contents"/>
        <result property="datetime" column="datetime"/>
        <result property="price" column="price"/>
        <result property="salePrice" column="sale_price"/>
        <result property="category" column="category"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByIsbn" parameterType="string" resultMap="BookResultMap">
        SELECT * FROM books WHERE isbn = #{isbn}
    </select>

    <insert id="insertBook" parameterType="com.geonpil.domain.entity.BookEntity" useGeneratedKeys="true" keyProperty="bookId" keyColumn = "book_id">
        INSERT INTO books (
        isbn, title, authors, publisher, thumbnail, contents,
        datetime, price, sale_price, category, status
        )
        VALUES (
        #{isbn}, #{title}, #{authors}, #{publisher}, #{thumbnail}, #{contents},
        #{datetime}, #{price}, #{salePrice}, #{category}, #{status}
        )
    </insert>


    <select id="findById" parameterType="long" resultMap="BookResultMap">
        SELECT *
        FROM books
        WHERE book_id = #{bookId}
    </select>

</mapper>
