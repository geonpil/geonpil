<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.geonpil.mapper.review.ReviewLikeMapper">

    <select id="existsByReviewIdAndUserId" resultType="boolean">
        SELECT EXISTS (
            SELECT 1 FROM review_likes WHERE review_id = #{reviewId} AND user_id = #{userId}
        )
    </select>

    <insert id="insertLike">
        INSERT INTO review_likes (review_id, user_id) VALUES (#{reviewId}, #{userId})
    </insert>

    <delete id="deleteLike">
        DELETE FROM review_likes WHERE review_id = #{reviewId} AND user_id = #{userId}
    </delete>

    <select id="countByReviewId" resultType="int">
        SELECT COUNT(*) AS likeCount FROM review_likes WHERE review_id = #{reviewId}
    </select>




</mapper>
